{
	"buildCommand": "pnpm --filter sorrento run build && cp -r sorrento-web/.vercel/output .vercel/output",
	"framework": null,
	"installCommand": "pnpm install",
	"outputDirectory": ".vercel/output"
}
